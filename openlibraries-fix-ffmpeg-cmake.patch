--- FindFFMPEG.cmake.orig	2009-01-28 10:13:04.000000000 +0100
+++ FindFFMPEG.cmake	2009-08-23 17:54:26.000000000 +0200
@@ -81,8 +81,8 @@
 ENDFOREACH( INC_ )
 
 # clear out duplicates
-LIST( REMOVE_DUPLICATES FFMPEG_INCLUDE_DIR )
-LIST( REMOVE_DUPLICATES FFMPEG_LIBRARY_DIR )
+#LIST( REMOVE_DUPLICATES FFMPEG_INCLUDE_DIR )
+#LIST( REMOVE_DUPLICATES FFMPEG_LIBRARY_DIR )
 
 # find the full paths of the libraries
 SET( TMP_ TMP-NOTFOUND )
